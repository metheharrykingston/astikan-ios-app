import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.astikan.user',
  appName: 'Astikan',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
